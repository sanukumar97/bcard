import 'package:flutter/material.dart';

class ShareCardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Share Card Page")
      )
    );
  }
}